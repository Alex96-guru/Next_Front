import React from "react";
import ReactDOM from "react-dom/client";
import AppReg from "./components_reg/app-reg.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <AppReg />
    </React.StrictMode>
);