import React from 'react';
import ReactDOM from 'react-dom/client';
import AppCode from './components_code/app-code.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <AppCode />
    </React.StrictMode>
);
