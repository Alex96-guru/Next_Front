import React from "react";
import ReactDOM from "react-dom/client";
import Applog from "./component_login/app-log.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <Applog />
    </React.StrictMode>
);